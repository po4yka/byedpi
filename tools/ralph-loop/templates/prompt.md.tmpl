You are implementing a byedpi Rust migration task through Ralph Orchestrator.

Run metadata:
- Run ID: {{RUN_ID}}
- Backend: {{BACKEND}}
- Workflow: {{WORKFLOW}}
- Task file: {{TASK_FILE}}
- Scratchpad: {{SCRATCHPAD_PATH}}
- Specs dir: {{SPECS_DIR}}

Execution requirements:
1. Read {{TASK_FILE}} first.
2. Read any `AGENTS.md` files you encounter and follow them. If none exist, follow the repository's existing conventions and test strategy.
3. Treat the C implementation as the behavior oracle unless the task explicitly changes that rule.
4. Keep changes scoped to the task; avoid unrelated refactors.
5. Run the verification steps listed in {{TASK_FILE}} before completion. If they are not enough for the touched files, add the narrowest extra verification needed.
6. Keep or expand the current contract, oracle-diff, and runtime parity coverage. Do not delete tests to make the task pass.
7. If blocked, explain the blocker and the next concrete action.
8. When complete and verified, print exactly: LOOP_COMPLETE
