# Ralph Loop Task

- Project: byedpi
- Run ID: {{RUN_ID}}
- Backend: {{BACKEND}}
- Workflow: {{WORKFLOW}}
- Created: {{CREATED_AT}}

## Repository Context

- Active Rust workspace crates: `ciadpi-packets`, `ciadpi-config`, `ciadpi-session`, `ciadpi-desync`, `ciadpi-bin`
- Current Rust binary target: `ciadpi`
- Active migration manifest: `tools/ralph-loop/tasks/byedpi-rust-migration.tsv`
- Current C runtime is retained only until the active backlog explicitly retires it
- Coverage spec: `tests/coverage_map.md`
- Scratchpad: `{{SCRATCHPAD_PATH}}`
- Ralph specs directory: `{{SPECS_DIR}}`
- Installed Ralph 2.7.0 on this machine enforces a fixed 14400-second max runtime, so the task must complete one verified slice and stop cleanly for relaunch

## Requested Task
